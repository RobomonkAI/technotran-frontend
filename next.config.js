/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["github.com", "raw.githubusercontent.com", "res.cloudinary.com"],
  },
};

module.exports = nextConfig;
